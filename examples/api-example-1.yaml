apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: cra-income-verification-api
  description: Secure API for verifying individual income data from Canada Revenue Agency (CRA) records, used for eligibility and fraud prevention in BC programs.
  tags:
    - secure-data-exchange
    - authoritative-data-register
    - eligibility-factor-verification
spec:
  type: openapi
  lifecycle: production
  owner: group:platform-team  # ← change to your actual owner
  system: income-assistance-system  # optional: group related APIs
  definition:
    $text: ./openapi.yaml